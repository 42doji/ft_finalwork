{
  "compilerOptions": {
    "target": "ES6",                     /* 컴파일될 JavaScript 버전 (ES6는 대부분의 최신 브라우저에서 지원) */
    "module": "ESNext",                  /* 모듈 시스템 (최신 ES 모듈 사용) */
    "outDir": "./dist",                  /* 컴파일된 JS 파일이 저장될 디렉토리 */
    "rootDir": "./src",                  /* TypeScript 소스 파일이 있는 루트 디렉토리 */
    "strict": true,                      /* 모든 엄격한 타입-검사 옵션 활성화 */
    "esModuleInterop": true,             /* ES 모듈과 CommonJS 모듈 간의 상호 운용성 활성화 */
    "skipLibCheck": true,                /* 모든 선언 파일(*.d.ts)의 타입 검사 스킵 */
    "forceConsistentCasingInFileNames": true, /* 파일 이름의 대소문자 일관성 강제 */
    "sourceMap": true,                   /* 디버깅을 위한 .map 파일 생성 */
    "moduleResolution": "node"           /* 모듈 해석 전략: 'node' 또는 'classic' */
  },
  "include": [
    "src/**/*"                           /* 컴파일할 파일 또는 폴더 패턴 */
  ],
  "exclude": [
    "node_modules",                      /* 컴파일에서 제외할 폴더 */
    "**/*.spec.ts"                       /* 테스트 파일 제외 (예시) */
  ]
}